using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace PractTask3
{
    public class CallSystem
    {
        public int IDDefiant()
        {
            using (CallSystemContext context =new CallSystemContext())
            {
                //var defiant=context.users.Where(u=>u.IDDefiant=)вывести ID где имя равно

            }
        }
    }
}
